# Coinbase API Credentials
# Get these from: https://www.coinbase.com/settings/api
# API key format: UUID or string identifier
# API secret: Base64-encoded secret key
CB_API_KEY=API Key UUID or string identifier
CB_API_SECRET=APi secret Key 64 encoded

# Database Configuration (optional)
# Path where SQLite state database will be stored
# Default: ./state/portfolio.db (local) or /app/state/portfolio.db (Docker)
# STATE_DB=./state/portfolio.db

# Database Encryption (optional)
# Strong password for encrypting the database at rest (requires sqlcipher3)
# If not set, database encryption is disabled
# DB_PASSWORD=your_strong_encryption_password_here

# Configuration File (optional)
# Path to trading configuration file (YAML)
# Default: ./config.yaml
# CONFIG_PATH=./config.yaml

# Web GUI Configuration (optional)
# Credentials for web dashboard access
# GUI_USER=admin
# GUI_PASS=your_secure_password
# GUI_SESSION_KEY=your_32_byte_session_key_for_encryption

# Logging (optional)
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
# LOG_LEVEL=INFO

# Paper Trading (optional)
# Set to 'true' for paper trading mode (no real orders)
# PAPER_TRADING=false

# Python Environment (optional)
# Set to 'production' to disable debug features
# ENVIRONMENT=development

# Docker/Container Settings (optional)
# Maximum number of concurrent positions
# MAX_POSITIONS=10

# API Timeout (seconds)
# REQUEST_TIMEOUT=30

# Webhook Secret (if using webhooks)
# WEBHOOK_SECRET=your_webhook_secret_here

# Monitoring/Alerting (optional)
# Slack webhook URL for alerts
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Email Alerts (optional)
# ALERT_EMAIL=your_email@example.com
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@example.com
# SMTP_PASSWORD=your_app_password
